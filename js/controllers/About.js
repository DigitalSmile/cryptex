function AboutController($scope) {
    $scope.headerTitle = "About Crypto Notes";
    $scope.aboutActive = "active";
    //var e = CryptoJS.AES.encrypt("A dialog is a floating window that contains a title bar and a content area. The dialog window can be moved, resized and closed with the 'x' icon by default. If the content length exceeds the maximum height, a scrollbar will automatically appear. A bottom button bar and semi-transparent modal overlay layer are common options that can be added.","pass");
   // console.log(e.toString());
   // var d = CryptoJS.AES.decrypt(e,"pass")
   // console.log(d.toString(CryptoJS.enc.Utf8));
}